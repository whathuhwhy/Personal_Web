import type { Metadata } from "next";
import { Geist, Geist_Mono, JetBrains_Mono, Fraunces } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
  display: "swap",
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
  display: "swap",
});

const jetbrainsMono = JetBrains_Mono({
  subsets: ["latin"],
  weight: ["400", "500"],
  variable: "--font-jetbrains-mono",
  display: "swap",
});

const fraunces = Fraunces({
  subsets: ["latin"],
  variable: "--font-fraunces",
  display: "swap",
  axes: ["opsz"],
});

export const metadata: Metadata = {
  title: "Daniel Napitu — Engineer & Builder",
  description:
    "Full-stack engineer and data systems architect. I build scalable cloud infrastructure and products that ship.",
  keywords: ["software engineer", "full stack", "AWS", "GCP", "Python", "Next.js"],
  authors: [{ name: "Daniel Napitu" }],
  openGraph: {
    title: "Daniel Napitu — Engineer & Builder",
    description: "Full-stack engineer and data systems architect.",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} ${jetbrainsMono.variable} ${fraunces.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">{children}</body>
    </html>
  );
}
